<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>🎉 Gọi Học Sinh Ngẫu Nhiên Vui Nhộn 🎲</title>
  <style>
    body {
      font-family: 'Comic Sans MS', cursive, Arial, sans-serif;
      background: linear-gradient(135deg, #ffe6f2, #ffb6c1);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      padding: 20px;
      overflow-x: hidden;
    }
    h1 {
      color: #d63384;
      margin-bottom: 15px;
      text-shadow: 1px 1px 4px #ff69b4;
      font-size: 2.8em;
    }
    textarea {
      width: 320px;
      height: 140px;
      font-size: 16px;
      padding: 12px;
      border-radius: 12px;
      border: 2px solid #ff69b4;
      resize: none;
      box-shadow: 0 0 10px #ff69b4;
      transition: border-color 0.3s;
    }
    textarea:focus {
      border-color: #d63384;
      outline: none;
      box-shadow: 0 0 15px #d63384;
    }
    button {
      margin-top: 20px;
      padding: 14px 32px;
      font-size: 20px;
      background-color: #d63384;
      color: white;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      box-shadow: 0 6px 15px rgba(214,51,132,0.7);
      transition: background-color 0.3s, transform 0.2s;
    }
    button:hover {
      background-color: #ff1493;
      transform: scale(1.05);
    }
    #result {
      margin-top: 40px;
      font-size: 48px;
      font-weight: 900;
      color: #d63384;
      min-height: 70px;
      text-align: center;
      text-shadow: 2px 2px 8px #ff69b4;
      animation: glow 2s ease-in-out infinite alternate;
      position: relative;
    }
    @keyframes glow {
      from {
        text-shadow: 0 0 10px #ff69b4, 0 0 20px #ff69b4;
        color: #ff1493;
      }
      to {
        text-shadow: 0 0 20px #ff1493, 0 0 40px #d63384;
        color: #d63384;
      }
    }
    .confetti {
      position: fixed;
      pointer-events: none;
      font-size: 24px;
      animation: fall 3s linear forwards;
      user-select: none;
    }
    @keyframes fall {
      0% {
        transform: translateY(-50px) rotate(0deg);
        opacity: 1;
      }
      100% {
        transform: translateY(100vh) rotate(360deg);
        opacity: 0;
      }
    }
  </style>
</head>
<body>
  <h1>🎉 Gọi Học Sinh Ngẫu Nhiên Vui Nhộn</h1>
  <textarea id="namesInput" placeholder="Nhập danh sách học sinh, mỗi tên 1 dòng..."></textarea>
  <button onclick="callRandom()">Gọi Ngẫu Nhiên!</button>
  <div id="result"></div>

  <script>
    function createConfetti(x, y) {
      const icons = ['🎉', '✨', '💥', '🌟', '🎈', '🎊', '🎀'];
      const confetti = document.createElement('div');
      confetti.classList.add('confetti');
      confetti.textContent = icons[Math.floor(Math.random() * icons.length)];
      confetti.style.left = x + 'px';
      confetti.style.top = y + 'px';
      confetti.style.fontSize = (Math.random() * 20 + 20) + 'px';
      document.body.appendChild(confetti);
      setTimeout(() => {
        confetti.remove();
      }, 3000);
    }

    function speak(text) {
      if (!window.speechSynthesis) return;
      const utterance = new SpeechSynthesisUtterance(text);
      utterance.lang = 'vi-VN';
      window.speechSynthesis.cancel();
      window.speechSynthesis.speak(utterance);
    }

    function callRandom() {
      const input = document.getElementById('namesInput').value.trim();
      if (!input) {
        alert('Vui lòng nhập danh sách tên học sinh!');
        return;
      }
      const names = input.split('\n').filter(name => name.trim().length > 0);
      const randomIndex = Math.floor(Math.random() * names.length);
      const selectedName = names[randomIndex];
      const resultDiv = document.getElementById('result');

      // Show name with glow animation
      resultDiv.textContent = selectedName;

      // Tạo confetti xung quanh tên
      for (let i = 0; i < 30; i++) {
        const x = window.innerWidth / 2 + (Math.random() - 0.5) * 200;
        const y = window.innerHeight / 2 + (Math.random() - 0.5) * 100;
        createConfetti(x, y);
      }

      // Đọc tên học sinh
      speak(selectedName);
    }
  </script>
</body>
</html>
